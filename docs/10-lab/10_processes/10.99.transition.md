---
layout: default
title: 10.99 Transition
parent: 10 Lab Processes
grand_parent: Lab Management
nav_order: 99
---

# 10.99 Transition

Pages that currenlty require an update for the transition:

{% assign pages_to_update = site.pages | where: "transition_status", "toupdate" %}
<ul>
{% for p in pages_to_update %}
  <li><a href="{{ p.url }}">{{ p.title }}</a></li>
{% endfor %}
</ul>

All pages that generally require an update for the transition:

{% assign pages_to_update = site.pages | where_exp: "p",
  "p.transition_status == 'toupdate' or p.transition_status == 'updated'" %}

<ul>
{% for p in pages_to_update %}
  <li><a href="{{ p.url }}">{{ p.title }}</a></li>
{% endfor %}
</ul>
