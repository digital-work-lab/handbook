
Getting the index works.

TODO : 

- get subpages
- get content of subpages


```json

{
  "openapi": "3.1.0",
  "info": {
    "title": "Digital-Work Lab Handbook RAG",
    "version": "1.3.2",
    "description": "Static RAG index published via GitHub Pages. Primary entry point is /rag/index.json. JSON chunk files are served from /rag/{path}. If a JSON chunk file is missing, the client can fetch the original Markdown from the handbook root."
  },
  "servers": [
    {
      "url": "https://digital-work-lab.github.io/handbook"
    }
  ],
  "paths": {
    "/rag/index.json": {
      "get": {
        "operationId": "getRagIndex",
        "summary": "Get top-level RAG index generated by GitHub Actions",
        "responses": {
          "200": {
            "description": "RAG index with sections or files",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "generated_at": { "type": "string" },
                    "model": { "type": "string" },
                    "files": {
                      "type": "array",
                      "description": "List of files that have prebuilt embeddings",
                      "items": {
                        "type": "object",
                        "required": ["path", "json_url", "md_url"],
                        "properties": {
                          "path": {
                            "type": "string",
                            "description": "Logical handbook path, e.g. 10-lab/index.md"
                          },
                          "json_url": {
                            "type": "string",
                            "description": "URL to prebuilt JSON under /rag/, e.g. /rag/10-lab/index.json"
                          },
                          "md_url": {
                            "type": "string",
                            "description": "Fallback: URL to original Markdown, e.g. /10-lab/index.md"
                          }
                        }
                      }
                    }
                  },
                  "required": ["files"]
                }
              }
            }
          }
        }
      }
    },
    "/rag/{path}": {
      "get": {
        "operationId": "getRagFile",
        "summary": "Fetch a prebuilt RAG JSON file as published on GitHub Pages under /rag/",
        "parameters": [
          {
            "name": "path",
            "in": "path",
            "required": true,
            "description": "Path to the RAG JSON file relative to /rag/, e.g. 10-lab/index.json",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "RAG JSON chunk file content",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "description": "Standardized RAG chunk format.",
                  "properties": {
                    "source": {
                      "type": "string",
                      "description": "Original handbook source path, e.g. 10-lab/index.md"
                    },
                    "generated_at": {
                      "type": "string",
                      "description": "Timestamp when this RAG JSON was generated"
                    },
                    "embedding_model": {
                      "type": "string",
                      "description": "Name of the embedding model used, if any"
                    },
                    "chunks": {
                      "type": "array",
                      "description": "List of text chunks extracted from the source",
                      "items": {
                        "type": "object",
                        "properties": {
                          "id": {
                            "type": "string",
                            "description": "Chunk identifier"
                          },
                          "text": {
                            "type": "string",
                            "description": "Chunk text content"
                          },
                          "metadata": {
                            "type": "object",
                            "description": "Additional metadata for this chunk",
                            "additionalProperties": true
                          },
                          "embedding": {
                            "type": "array",
                            "description": "Optional precomputed embedding vector",
                            "items": {
                              "type": "number"
                            }
                          }
                        },
                        "required": ["text"]
                      }
                    },
                    "metadata": {
                      "type": "object",
                      "description": "File-level metadata",
                      "additionalProperties": true
                    }
                  },
                  "required": ["chunks"]
                }
              }
            }
          },
          "404": {
            "description": "RAG JSON not found under /rag/"
          }
        }
      }
    },
    "/{path}": {
      "get": {
        "operationId": "getHandbookFile",
        "summary": "Fetch a raw handbook file (Markdown) as published on GitHub Pages root",
        "parameters": [
          {
            "name": "path",
            "in": "path",
            "required": true,
            "description": "Path to the published Markdown file relative to the handbook root, e.g. 10-lab/index.md",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "Raw Markdown file content",
            "content": {
              "text/markdown": {
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "404": {
            "description": "File not found on GitHub Pages"
          }
        }
      }
    }
  }
}


```